datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Invite {
  id                Int       @id @default(autoincrement())
  familyName        String
  inviteToken       String    @unique
  category          String
  maxGuests         Int
  invitationOpened  Boolean   @default(false)
  rsvpSubmitted     Boolean   @default(false)
  side              String    @default("Neutral") // Bride, Groom, Neutral
  seatingPublished  Boolean   @default(false)
  createdAt         DateTime  @default(now())
  rsvp              RSVP?
}

model RSVP {
  id                Int       @id @default(autoincrement())
  inviteId          Int       @unique
  attendanceCount   Int
  serialNumber      String    @unique
  qrCode            String
  checkedIn         Boolean   @default(false)
  checkedInAt       DateTime?
  checkInPhoto      String?
  checkedOut        Boolean   @default(false)
  checkedOutAt      DateTime?
  anyChildren       Boolean   @default(false)
  childrenCount     Int       @default(0)
  createdAt         DateTime  @default(now())
  invite            Invite    @relation(fields: [inviteId], references: [id], onDelete: Cascade)
  attendees         Attendee[]
}

model Attendee {
  id              Int      @id @default(autoincrement())
  rsvpId          Int
  fullName        String
  phoneNumber     String?
  serialNumber    String   @unique
  attendeeToken   String   @unique
  registeredBy    String?
  checkedIn       Boolean   @default(false)
  checkedInAt     DateTime?
  checkInPhoto    String?
  checkedOut      Boolean   @default(false)
  checkedOutAt    DateTime?
  tableId         Int?
  table           Table?   @relation(fields: [tableId], references: [id], onDelete: SetNull)
  seatNumber      String?
  createdAt       DateTime @default(now())
  rsvp            RSVP     @relation(fields: [rsvpId], references: [id], onDelete: Cascade)
}

model GalleryPhoto {
  id              Int       @id @default(autoincrement())
  uploadedBy      String?
  imageUrl        String
  cloudinaryId    String
  approved        Boolean   @default(false)
  createdAt       DateTime  @default(now())
}

model Donation {
  id              Int       @id @default(autoincrement())
  donorName       String?
  amount          Float
  reference       String    @unique
  status          String
  createdAt       DateTime  @default(now())
}

model Admin {
  id              Int       @id @default(autoincrement())
  username        String    @unique
  password        String
  role            String
  createdAt       DateTime  @default(now())
}

model Table {
  id          Int        @id @default(autoincrement())
  name        String     @unique
  capacity    Int
  side        String     @default("Neutral") // Bride, Groom, Neutral
  category    String?    // E.g. VIP, Bride's family, etc.
  createdAt   DateTime   @default(now())
  attendees   Attendee[]
}
